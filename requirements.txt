--extra-index-url https://download.pytorch.org/whl/cu126
accelerate
peft
ftfy
torch==2.11.0
torchaudio==2.11.0
torchvision==0.26.0
torchcodec>=0.14,<0.15  # ABI-совместим с torch 2.11
pyannote.audio>=4.0
omegaconf
fastapi[standard]
transformers
# Токенизатор T5 у Chroma лежит как spiece.model — нужен sentencepiece, а
# protobuf требуется его конвертеру в быстрый токенизатор. tiktoken добавлен
# потому, что без sentencepiece transformers падает именно с требованием
# tiktoken (сообщение вводит в заблуждение, но пусть будет оба бэкенда).
sentencepiece
protobuf
tiktoken
whisperx
dotenv
bitsandbytes  # nf4-квантизация Wan для diffusers-отката (USE_COMFYUI=False)
requests  # HTTP-клиент к ComfyUI
imageio
imageio-ffmpeg
git+https://github.com/huggingface/diffusers
